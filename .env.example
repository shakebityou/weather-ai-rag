# LLM：支持任意 OpenAI 兼容接口（DeepSeek / 通义 / 智谱 / OpenAI）
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-你的key
LLM_MODEL=deepseek-chat

# Redis（没有可留默认，会自动降级为不缓存）
REDIS_URL=redis://localhost:6379/0
